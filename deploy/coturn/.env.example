# 이 파일을 .env로 복사한 뒤 값을 채우세요: cp .env.example .env
# (참고용 메모입니다. 실제 값은 turnserver.conf에 직접 반영합니다.)

# 서버 공인 IP (클라우드면 external-ip=<공인>/<사설> 형식 참고)
PUBLIC_IP=203.0.113.10

# realm (임의 문자열, 보통 도메인)
REALM=turn.example.com

# 고정 계정 자격증명
TURN_USER=remoteuser
TURN_PASS=change-me-strong-password
